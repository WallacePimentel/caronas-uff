<%= form_with(model: carpool, local: true) do |form| %>
  <% if carpool.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carpool.errors.count, "error") %> prohibited this carpool from being saved:</h2>

      <ul>
        <% carpool.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :beginning_campus_id %>
    <%= form.text_field :beginning_campus_id %>
  </div>

  <div class="field">
    <%= form.label :ending_campus_id %>
    <%= form.text_field :ending_campus_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
