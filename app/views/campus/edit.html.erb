<div class="container mt-5 ml-2">
    <h1><strong><%= t('editing') %> <%= Campu.model_name.human(count: 1) %></strong></h1>

    <%= render 'form', campu: @campu %>

    <%= link_to t('links.show'), @campu , class: "btn btn-success" %> |
    <%= link_to t('links.back'), campus_path, class: "btn btn-secondary text-light" %>
</div>